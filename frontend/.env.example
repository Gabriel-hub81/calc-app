# Copia a .env.local y llena los valores. NUNCA commitear .env.local.

VITE_BACKEND_URL=http://localhost:8080

# Wallet embebido no-custodial (Privy). Sin App ID, la app corre en modo demo:
# todo funciona y el login simula una cuenta local (backend en AUTH_MODE=dev).
VITE_PRIVY_APP_ID=

# Solana (links al explorer del comprobante)
VITE_SOLANA_NETWORK=devnet
VITE_CALC_PROGRAM_ID=Gh3WhpAtc8HDUUU59amgzaxvg4nCV3V4pSN3EntBHwRL
